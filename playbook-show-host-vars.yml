---
- name: Display all variables/facts for the current host
  hosts: all
  gather_facts: true
  tasks:
    - name: Print all host variables and facts
      ansible.builtin.debug:
        msg: "{{ hostvars[inventory_hostname] }}"
